@*@page "/BP"*@
@using BlazorSurf.Shared
@using Blazored.Modal;
@using Blazored.Modal.Services;
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.WebAssembly.Authentication


@*<BoardPage @bind-SelectedBoard="board" />*@
@*<TwoWayBinding @bind-value="SelectedBoard"/>*@

@*@if (Name == null && 1 != 1) // delete
{
    <p><em>No data</em></p>
}
else
{*@
    <div>
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>:(</td>
                    <td>@SelectedItem.Name</td>
                </tr>
            </tbody>
        </table>
    </div>
@*}*@

 pog?

@code {
    [Parameter] public Board? SelectedItem { get; set; }

    protected override void OnInitialized()
    {

    }

}